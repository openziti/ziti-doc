import TunnelerOverviewMd from './_tunneler-overview.mdx';

# Tunnelers

<TunnelerOverviewMd />

Each tunneller operates similarly. The goal is to have the tunneler seamlessly intercecpt traffic destined for Ziti
services and forward that traffic over the Ziti overlay instead of the underlay network.
